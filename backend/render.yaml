services:
  - type: web
    name: under-pressure-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ALLOWED_ORIGINS
        value: "https://pressurelab-ai.vercel.app,https://pressurelab-ai-e76j.vercel.app,http://localhost:5173"
      - key: ENVIRONMENT
        value: production
      - key: FOOTBALL_DATA_API_KEY
        value: ""
